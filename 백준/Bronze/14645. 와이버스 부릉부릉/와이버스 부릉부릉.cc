#include <iostream>
using namespace std;

int main() {
    int N, M;
    cin >> N >> M;

    for (int i = 0; i < M; i++) {
        int off, on;
        cin >> off >> on;
    }

    cout << "비와이" << '\n';
    return 0;
}